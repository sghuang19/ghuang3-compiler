/* Mixed declarations */

a: integer;
b: float;
c: string;
d: boolean;

func: function void (arg: integer) = { return; }

func: function void (arg: integer) = { return; }

main: function void () = {
    func(a, e);
    f: float;
    g: string;
    h: boolean;
    a + b + c + d + f + g + h;
    return i + j + k;
}
