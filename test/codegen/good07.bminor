/* Mixed precedence */
x: integer = x + y * 2;
x: integer = (x + y) * 2;
x: integer = (1 + 2 * (x + y)) * 2;
x: integer = ( f(1, 2) + x[0] ) ^ 1 * 2 / 2;
x: integer = a || b && c;
x: integer = a && b || c;
x: integer = a && (b || c);
